# Documentation

Use this folder for workspace-level documentation, workflows, decisions, and references.

Company-specific context belongs in `company/`. Project/repository registry belongs in `projects/`.
