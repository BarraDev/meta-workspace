# Optional SDD/Kiro Workspace

This folder is reserved for optional SDD/Kiro specs, steering files, and settings.

Install or update SDD support with:

```bash
mw sdd install
```

This workspace uses `cc-sdd` as the source for SDD skills and templates.
