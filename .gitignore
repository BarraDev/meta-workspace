# Local environment and secrets
.env
.env.*
!.env.example
*.local
secrets/

# Runtime/cache
.cache/
.tmp/
tmp/
logs/
*.log

# Agent/runtime output (deployable workspace lives under template/)
.pi/side-agents/runtime/
template/.pi/side-agents/runtime/
.claude/.cache/
template/.claude/.cache/
.codex/sessions/
template/.codex/sessions/
HANDOFF.md

# Rust build artifacts (Cargo.lock IS committed for the binary crate).
# The crate manifest is at the repo root, so target/ is here too.
/target/

# Dependency/vendor folders
node_modules/
.venv/
venv/

# OS/editor
.DS_Store
.idea/
.vscode/

# Optional generated backups
*.bak
*.backup
.cc-sdd-backup*/
